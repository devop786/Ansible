---
- hosts: k8s-setup
  vars_files:
    - ../../vars/k8s_vars.yaml  # Corrected spelling
  roles:
    - ../../roles/weavenetwork # Role name should match the one inside `roles/`
    #  - ../../roles/masternodesetup
    #    - ../../roles/slavenodejoin
     


